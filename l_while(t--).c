#include <stdio.h>

int main() {
    int t = 5; // Initialize t with a value
    
    while(t--) {
        printf("Value of t: %d\n", t);
    }
    
    printf("Loop ended.\n");
    return 0;
}